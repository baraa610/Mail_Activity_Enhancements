# mail_activity_enhancements

Enhances `mail.activity` with:
- `date_start` (Datetime)
- `duration_days` (stored computed)
- "Upcoming (Mine)" search filter
- Daily reminders (cron) for activities starting today (per-user timezone)

## Install
1. Put the module folder `mail_activity_enhancements` into your `addons` path.
2. Update Apps list and install the module or run:

